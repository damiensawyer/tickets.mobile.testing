import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'au.org.tickets.mobile',
  appName: 'tickets.mobile',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
